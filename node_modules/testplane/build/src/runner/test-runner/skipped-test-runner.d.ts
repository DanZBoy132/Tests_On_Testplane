export = SkippedTestRunner;
declare class SkippedTestRunner extends RunnableEmitter {
    constructor(test: any);
    _test: any;
    run(): void;
    _isSilentlySkipped({ silentSkip, parent }: {
        silentSkip: any;
        parent: any;
    }): any;
}
import { RunnableEmitter } from "../types";
