import { TestStatus } from "../../../../../../constants";
import { ImageEntity } from "../../../../types/store";
export declare enum EntityType {
    Group = "group",
    Suite = "suite",
    Browser = "browser"
}
export interface TreeViewItemData {
    id: string;
    entityType: EntityType;
    entityId: string;
    prefix?: string;
    title: string[];
    status: TestStatus | null;
    tags: string[];
    errorTitle?: string;
    errorStack?: string;
    images?: ImageEntity[];
    parentData?: TreeViewItemData;
    skipReason?: string;
    browserId?: string;
    stateName?: string;
}
export interface TreeRoot {
    isRoot: true;
    data?: TreeViewItemData;
    children?: TreeNode[];
}
export interface GenericTreeViewItem<T> {
    parentNode?: TreeRoot | GenericTreeViewItem<T>;
    data: T;
    children?: GenericTreeViewItem<T>[];
}
export type TreeNode = GenericTreeViewItem<TreeViewItemData>;
export declare const isTreeRoot: (nodeOrRoot: TreeNode | TreeRoot) => nodeOrRoot is TreeRoot;
