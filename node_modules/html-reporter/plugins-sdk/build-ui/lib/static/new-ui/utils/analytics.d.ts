import { DataForStaticFile } from "../../../server-utils";
declare global {
    interface Window {
        data?: DataForStaticFile;
    }
}
export declare const getAreAnalyticsEnabled: () => boolean;
export declare const getCounterId: () => number;
