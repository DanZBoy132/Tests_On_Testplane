import { ReactNode } from 'react';
export interface AttemptPickerItemProps {
    resultId: string;
    isActive?: boolean;
    onClick?: () => unknown;
    title?: string;
}
export declare const AttemptPickerItem: (props: AttemptPickerItemProps) => ReactNode;
