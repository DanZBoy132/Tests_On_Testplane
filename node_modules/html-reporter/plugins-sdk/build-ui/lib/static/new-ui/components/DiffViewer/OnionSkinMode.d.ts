import { ReactNode } from 'react';
import { ImageFile } from "../../../../types";
interface OnionSkinModeProps {
    expected: ImageFile;
    actual: ImageFile;
}
export declare function OnionSkinMode(props: OnionSkinModeProps): ReactNode;
export {};
