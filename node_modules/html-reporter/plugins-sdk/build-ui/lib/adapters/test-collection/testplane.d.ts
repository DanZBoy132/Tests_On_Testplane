import { TestplaneTestAdapter } from '../test/testplane';
import type { TestCollectionAdapter } from './';
import type { Config, TestCollection } from 'testplane';
export declare class TestplaneTestCollectionAdapter implements TestCollectionAdapter {
    private _testCollection;
    private _testAdapters;
    static create<T>(this: new (testCollection: TestCollection, saveHistoryMode?: Config['saveHistoryMode']) => T, testCollection: TestCollection, saveHistoryMode?: Config['saveHistoryMode']): T;
    constructor(testCollection: TestCollection, saveHistoryMode?: Config['saveHistoryMode']);
    get original(): TestCollection;
    get tests(): TestplaneTestAdapter[];
}
