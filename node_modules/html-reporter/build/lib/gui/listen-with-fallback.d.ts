import type { Express } from 'express';
interface ListenOptions {
    server: Express;
    hostname?: string;
    requestedPort?: number;
}
interface ListenResult {
    actualPort: number;
    hostnameForUrl: string;
}
export declare const listenWithFallback: ({ server, hostname, requestedPort }: ListenOptions) => Promise<ListenResult>;
export {};
