import { ToolAdapter } from '../adapters/tool';
interface MergeOptions {
    destPath: string;
    headers: string[];
}
export declare const mergeReports: (toolAdapter: ToolAdapter, srcPaths: string[], { destPath, headers }: MergeOptions) => Promise<void>;
export {};
